# ─── Database ───────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fivem_dev"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/fivem_dev"

# ─── Server ─────────────────────────────────────────────────
ORCHESTRATOR_PORT=3001
NODE_ENV=development

# ─── Auth ───────────────────────────────────────────────────
JWT_SECRET="dev-secret-change-in-production-min-32-chars"
CLERK_JWKS_DOMAIN="relevant-ram-9120.clerk.accounts.dev"
CLERK_JWT_AUDIENCE=""

# ─── CORS (comma-separated — add production origins later) ──
CORS_ORIGINS="http://localhost:3000,http://localhost:1420,http://tauri.localhost,tauri://localhost,https://tauri.localhost"

# ─── AI (OmniRoute — change to tunnel URL when hosted) ─────
OMNIROUTE_BASE_URL=http://localhost:20128/v1
OMNI_KEY=omni-key

# ─── Anthropic (fallback — leave empty if not used) ────────
ANTHROPIC_API_KEY=
