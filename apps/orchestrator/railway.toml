[build]
# Skip build step - tsx runs TypeScript directly
builder = "nixpacks"
buildCommand = "exit 0"

[deploy]
# Run TypeScript directly via tsx (no build step)
startCommand = "pnpm --filter @fivem-ai/orchestrator exec tsx src/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
