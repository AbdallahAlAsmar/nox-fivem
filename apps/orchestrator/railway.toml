[build]
builder = "docker"
dockerfile = "railway/Dockerfile"

[deploy]
# Use tsx to run TypeScript directly (no build step needed)
start_command = "pnpm --filter @fivem-ai/orchestrator exec tsx src/index.ts"
restart_policy_type = "ON_FAILURE"
restart_policy_max_retries = 3
